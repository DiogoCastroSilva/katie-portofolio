# @katie-portofolio/publications

Publications content library (TBD).

Add `.md` files under `src/md/` using the same frontmatter as projects. Run `npx nx run main:sync-content-assets` after adding cover images.

The `/publications` page in the app will list items from `getAllPublications()` once content is added.
