{
  "name": "@katie-portofolio/main",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "sync-post-assets": {
        "executor": "nx:run-commands",
        "options": {
          "command": "node tools/sync-post-assets.mjs",
          "cwd": "{workspaceRoot}"
        }
      },
      "build": {
        "dependsOn": ["sync-post-assets"],
        "options": {
          "command": "next build --webpack"
        }
      },
      "dev": {
        "dependsOn": ["sync-post-assets"],
        "options": {
          "command": "next dev --webpack"
        }
      }
    }
  },
  "dependencies": {
    "@katie-portofolio/navigation": "*",
    "@katie-portofolio/posts": "*",
    "next": "16.2.3",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  }
}
